pyspark==3.5.*
python-dotenv==1.0.*
pytest==8.2.*
pytest-cov==5.0.*
streamlit==1.35.*
plotly==5.22.*

# apache-airflow: apenas para referência.
# Instalação via pip puro costuma quebrar (exige constraints-file oficial
# por conflito de dependências). O Airflow real roda via Docker Compose.
# apache-airflow==2.9.*
